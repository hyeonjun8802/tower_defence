# PLANET RIFT DEFENSE — MOBILE UX/UI OPTIMIZATION UPDATE

## Version
v50 Mobile Optimization Patch

---

# 핵심 변경 사항

이번 패치는 기존 PC 중심 레이아웃을 모바일 플레이 환경에 맞게 재구성하는 것을 목표로 진행되었다.

기존 문제:
- 전투 보드가 너무 작게 표시됨
- 하단 UI 패널이 화면을 과도하게 차지함
- 버튼 크기가 커서 플레이 영역이 좁아짐
- 모바일 Safari 환경에서 프레임 드랍 및 버벅임 발생
- Glow / Blur / Particle 효과가 과도하여 GPU 부하 증가
- 모바일에서 정보 밀도가 높아 피로감 발생

---

# MOBILE UX 개선

## 1. 전투 보드 확대
- 모바일에서 셀 및 보드 영역 확대
- 전투 공간 가시성 강화
- 플레이어 시선 집중도 향상

## 2. 전투 영역 상단 이동
- 보드를 화면 상단 쪽으로 이동
- 하단 UI 공간 확보
- 실제 플레이 영역 체감 증가

## 3. 하단 UI Compact화
- 패널 높이 축소
- padding / margin 감소
- 버튼 높이 축소
- 모바일 조작 최적화

## 4. 스킬 카드 Compact UI
- 카드 width 제한
- 텍스트 크기 조정
- 모바일 세로 화면 최적화
- 긴 설명 영역 최소화

---

# MOBILE PERFORMANCE MODE

모바일 환경(window width <= 768)에서 자동 적용된다.

## 최적화 항목

### 비활성 또는 축소
- Particle 감소
- Beam glow 감소
- Background animation 감소
- Damage text 동시 출력 제한
- Blur 효과 최소화
- Backdrop-filter 제거 또는 축소
- Shadow 효과 단순화
- Floating animation 감소

### 렌더링 최적화
- requestAnimationFrame 업데이트 최소화
- repaint/reflow 감소
- animation duration 단축
- 모바일 GPU 부하 감소

---

# SAFARI / iPHONE 대응

## 적용 사항
- 100vh 대신 100dvh 사용
- safe-area-inset-bottom 대응
- iPhone 브라우저 UI 가림 현상 완화
- 모바일 viewport 안정성 향상

---

# GAMEPLAY 방향성

현재 게임 방향성은 아래를 유지한다.

## 핵심 컨셉
- 장판 기반 전략 디펜스
- 행성 성장/합성
- 랜덤 성장 요소
- 행성별 시너지
- 광역/빙결/중독/전기/블랙홀 등 속성 기반 전투

## 난이도 방향
- 초반부터 장판 활용이 중요
- 단순 화력보다 전략 배치 요구
- 랜덤 강화로 리플레이성 확보

---

# 향후 개선 예정

## UI
- 손가락 조작 기반 터치 UX 개선
- 드래그 합성 반응 개선
- 햅틱 대응

## 성능
- Canvas 렌더링 분리
- OffscreenCanvas 검토
- Mobile GPU 대응 최적화
- Sprite Atlas 경량화

## 게임성
- 환경 디버프 타일
- 행성 특수 이벤트
- 보스 패턴 강화
- 시너지 조합 강화

---

# 결론

이번 패치는 모바일 플레이 환경 기준으로:
- 가독성
- 조작성
- 성능
- 화면 밀도

를 개선하는 데 초점을 맞춘 UX/UI 최적화 패치이다.


---

# 추가 패치: Mobile App Compact UI

## 목적
모바일 앱처럼 보이도록 전투 보드는 유지하되, 화면 위에 올라가는 UI 요소를 더 작고 얇게 조정했다.

## 변경 사항
- 하단 패널 높이 추가 축소
- 버튼 높이와 폰트 크기 축소
- 스탯 카드 padding / gap 축소
- 행성 카드 크기 축소
- 스킬 버튼 compact화
- 모달/스킬 선택 카드 크기 축소
- 스테이지 선택 UI 축소
- 모바일에서 shadow / text-shadow / nebula 효과 추가 제거

## UX 기준
- 전투 보드: 크게 유지
- 정보 UI: 작고 얇게 표시
- 버튼: 앱 하단 컨트롤처럼 compact하게 표시
- 카드/팝업: 세로 화면 기준으로 과도하게 커지지 않도록 제한

## 적용 기준
- max-width: 768px
- max-width: 390px 초소형 화면 추가 대응


---

# 추가 패치: Mobile Medium UI + Board Upward Shift

## 목적
이전 compact UI가 너무 작아 보이는 문제를 보완하여, 원본과 compact의 중간 크기로 조정했다. 동시에 전투 지도/블록 영역을 위쪽으로 이동해 실제 플레이 영역이 아래로 처져 보이는 문제를 개선했다.

## 변경 사항
- 전투 지도 영역을 약 30px 이상 상단으로 이동
- 하단 UI는 원본보다 작게, compact 버전보다 크게 조정
- 행성 구매 카드 중간 크기 적용
- 스탯 패널, 버튼, 폰트 크기 중간값 적용
- 랜덤 스킬 선택 팝업 축소
- 스킬 카드 크기와 텍스트 크기 축소
- 초소형 모바일 화면 기준 별도 보정 유지

## UX 기준
- 보드/블록: 위쪽으로 이동하여 게임 화면 중심 확보
- 하단 UI: 앱처럼 과하지 않게, 하지만 너무 작지 않게
- 랜덤 스킬 UI: 플레이 흐름을 가리지 않도록 중간 크기로 축소


---

# 추가 패치: Canvas Internal Board Up Shift

## 배경
이전 수정은 CSS 기준으로 `#field` 위치를 올렸지만, 실제 게임 맵은 고정 크기 canvas 내부 좌표계로 렌더링되고 있어 체감 위치가 거의 바뀌지 않았다.

## 변경 사항
- 모바일 기준 내부 canvas 좌표 `GY`를 120에서 78로 조정
- 모바일 기준 `CORE` 위치도 함께 상단 보정
- CSS 위치 이동은 과도하지 않게 축소
- 랜덤 스킬 선택 모달을 중간 크기로 고정 보정

## 결과
모바일/태블릿 화면에서 지도, 블록, 경로, 코어가 실제 렌더링 기준으로 위쪽에 배치된다.


---

# 추가 패치: Stage Map Scale + Random Skill Font

## 목적
스테이지 맵에서 하단 설명 패널이 지도 대부분을 가리는 문제를 줄이고, 랜덤 스킬 선택 UI의 폰트와 카드 크기를 모바일 기준으로 더 작게 조정했다.

## 변경 사항
- 모바일 스테이지 노드 크기 축소
- 스테이지 맵 타이틀/상태 UI 축소
- 스테이지 설명 패널 높이/폰트/패딩 축소
- 보스 카드, 태그, 위험도 표시 축소
- ENTER 버튼 폭/폰트 축소
- 랜덤 스킬 선택 카드 크기 축소
- 랜덤 스킬 카드 제목/설명/배지/아이콘 폰트 축소
- 390px 이하 초소형 화면 추가 보정


---

# 추가 패치: Mobile Square Board + Lowered Board Position

## 목적
전투 보드가 너무 위로 붙어 보이는 문제와 모바일 반응형에서 블록이 세로/가로로 찌그러져 보이는 문제를 개선했다.

## 변경 사항
- 모바일 내부 보드 Y 좌표를 78에서 96으로 낮춤
- 모바일 코어 Y 좌표도 함께 하단 보정
- `#field`에 원본 canvas 비율 748:708 유지 적용
- canvas `aspect-ratio` 고정
- 보드가 화면에 맞춰 줄어들더라도 블록 정사각형 비율 유지
- 하단 UI width도 보드 비율에 맞춰 정렬
- 상단 성장 알림 위치 소폭 조정

## 결과
모바일에서 블록이 찌그러지지 않고, 지도 위치가 상단에 과하게 붙지 않도록 조정됨.


---

# 추가 패치: Mobile HUD Refinement

## 목적
모바일 전투 화면 하단의 스테이지/재화/EXP/LEVEL UI가 PC 카드처럼 크게 보이는 문제를 개선했다.

## 변경 사항
- 하단 HUD 전체 높이 축소
- 스테이지 정보 패널 높이 축소
- Crystal / Core HP / EXP / Level 카드 축소
- 숫자 폰트 크기 축소
- 버튼 높이 및 폰트 축소
- Tactical Plates 영역 축소
- 행성 구매 카드 약간 축소
- 390px 이하 화면 추가 보정

## 결과
모바일에서 하단 UI가 게임 화면을 덜 차지하고, 보드/전투 영역 가시성이 좋아짐.


---

# 추가 패치: Mobile HUD No-Clip

## 목적
하단 HUD를 compact하게 줄이는 과정에서 아래 버튼 줄이 화면 하단에서 잘리는 문제를 수정했다.

## 변경 사항
- 모바일 하단 HUD 영역 높이 재확보
- `#side` overflow clipping 완화
- 전투 보드 영역을 하단 HUD 높이에 맞춰 자동 축소
- 버튼 두 줄이 화면 안에 들어오도록 높이 보정
- 390px 이하 화면 별도 보정

## 결과
모바일에서 하단 버튼과 Tactical Plates 영역이 잘리지 않고 표시된다.


---

# 추가 패치: Mobile Planet Card Text No-Overlap

## 목적
모바일 행성 구매 카드에서 설명 텍스트와 MAX/LV 배지가 겹치고 잘리는 문제를 수정했다.

## 변경 사항
- 모바일 행성 카드에서 상세 설명 텍스트 숨김
- 행성명은 한 줄 말줄임 처리
- COST는 오른쪽 상단 고정
- MAX/LV 배지는 오른쪽 하단 고정
- 카드 내부 overflow 정리
- 390px 이하 화면 별도 보정

## 결과
행성 카드에서 텍스트가 서로 겹치지 않고, 모바일에서는 핵심 정보만 표시된다.


---

# 추가 패치: Mobile Tiny Planet Level Badge

## 목적
모바일 행성 카드에서 MAX/LV 배지가 행성명을 가리는 문제를 수정했다.

## 변경 사항
- MAX/LV 배지를 보조 정보 수준으로 축소
- 행성명 표시 영역 확대
- 레벨 배지 오른쪽 아래 고정
- 작은 화면 기준 추가 축소

## 결과
행성명이 우선적으로 보이고, 레벨 정보는 작은 보조 배지로 표시된다.


---

# 추가 패치: Mobile Wide Planet Card

## 목적
모바일 행성 카드에서 행성명이 잘리고 행성 이미지가 카드 경계에 닿아 잘려 보이는 문제를 수정했다.

## 변경 사항
- 모바일 행성 카드 가로폭 확대
- 행성 아이콘 안전 여백 확보
- 행성 이미지 background-size를 contain 기준으로 보정
- 행성명 표시 영역 확대
- COST / MAX LV 배지는 오른쪽 고정 유지
- 가로 스크롤 카드 구조 유지

## 결과
행성 카드 내부 요소가 겹치거나 잘리지 않고, 카드가 가로 스크롤로 자연스럽게 표시된다.


---

# 추가 패치: Mobile Large Block / Tap Target

## 목적
모바일에서 블록과 행성이 너무 작아 보여 조작이 어려운 문제를 개선했다.

## 변경 사항
- 모바일 기준 내부 CELL 크기를 56에서 62로 확대
- 보드 좌표를 확대된 셀 기준으로 재정렬
- 모바일 코어 위치 보정
- 보드 표시 크기 소폭 확대
- 터치 조작 가시성 개선

## 결과
모바일에서 행성과 블록이 더 크게 보이고, 터치 조작이 쉬워진다.


---

# 추가 패치: Mobile Planet Card Rebuild

## 목적
모바일 행성 카드에서 아이콘이 여러 개로 보이고, 행성명이 계속 잘리는 문제를 수정했다.

## 변경 사항
- 행성 카드 가로폭을 204px 기준으로 확대
- liveThumb 스프라이트 표시를 `background-size: 500% 100%`로 복구
- 일반 썸네일은 cover 기준으로 표시
- 행성명 표시 영역 확대
- MAX/LV 배지는 작은 보조 정보로 유지
- 모바일 행성 카드는 가로 스크롤 카드로 안정화

## 결과
행성 아이콘이 정상적으로 한 개만 표시되고, 행성명이 잘리지 않도록 개선됨.


---

# 추가 패치: Mobile Mini Planet Card + Sprite Bleed Fix

## 목적
모바일에서 행성 구매 카드가 여전히 크게 보이고, 일부 행성 썸네일 오른쪽에 다음 스프라이트 프레임이 살짝 보이는 문제를 수정했다.

## 변경 사항
- 행성 구매 카드 크기 추가 축소
- 행성명, COST, MAX/LV 배지 폰트 축소
- MAX/LV 배지를 더 작은 보조 정보로 변경
- liveThumb 스프라이트 표시 영역을 확대해 옆 프레임 노출 감소
- 썸네일 overflow mask 유지
- 하단 HUD도 한 단계 더 축소

## 결과
행성 구매 카드는 보조 UI처럼 작아지고, 썸네일에서 옆 프레임이 보이는 현상이 줄어든다.


---

# 추가 패치: Mobile Skill Modal Designer Alignment

## 목적
랜덤 스킬 선택 UI에서 텍스트가 어색하게 줄바꿈되고 카드 내부 정렬이 맞지 않는 문제를 디자이너 관점으로 재정렬했다.

## 변경 사항
- 카드 높이/여백 재설계
- 타이틀/아이콘/설명 정렬 통일
- 한글 단어 개행 최소화 (`word-break: keep-all`)
- COMMON 배지 위치 재정렬
- 하단 패시브 태그 중앙 정렬
- 모바일 카드 비율 프리미엄 스타일로 보정

## 결과
랜덤 스킬 카드가 모바일에서도 정렬감 있게 보이고, 텍스트가 덜 깨져 보이도록 개선됨.


---

# 추가 패치: Mobile Planet Sprite X-Offset

## 목적
모바일 행성 카드 썸네일 오른쪽에 옆 스프라이트 프레임이 아주 미세하게 보이는 문제를 보정했다.

## 변경 사항
- liveThumb background-position-x를 오른쪽으로 소폭 이동
- sprite background-size를 더 크게 조정
- 썸네일 오른쪽 안쪽 마스킹 강화
- 390px 이하 화면 별도 보정

## 결과
행성 썸네일 오른쪽의 미세한 스프라이트 경계 노출이 줄어든다.


---

# 추가 패치: Mobile Planet Icon Square Alignment

## 목적
행성 썸네일이 가로로 길어 보이고 중앙 정렬이 어긋나 보이는 문제를 수정했다.

## 변경 사항
- 행성 썸네일을 정사각형 마스크로 변경
- liveThumb sprite 가로 과확대 제거
- background-position을 중앙 기준으로 재보정
- 오른쪽 마스킹 강도를 줄여 아이콘 왜곡 완화
- 390px 이하 화면 별도 보정

## 결과
행성 아이콘이 납작해 보이지 않고, 카드 안에서 더 자연스럽게 중앙 정렬된다.


---

# 추가 패치: Specific Planet Sprite Offset

## 목적
특정 행성(솔라/광자/히든)만 오른쪽 스프라이트 경계가 보이는 문제를 개별 보정했다.

## 변경 사항
- 솔라 행성 X축 우측 이동
- 광자 행성 X축 우측 이동
- 히든 행성 X축 우측 이동

## 결과
문제가 있던 특정 행성만 프레임 중앙 정렬이 개선됨.


---

# 추가 패치: Final Data-Type Sprite Bleed Fix

## 목적
행성 카드 실제 마크업이 `data-type` 기준으로 생성되어, 기존 이름 기반 CSS 보정이 일부 적용되지 않는 문제를 수정했다.

## 변경 사항
- 실제 카드 구조 기준 `data-type="0"` 솔라 보정
- `data-type="5"` 광자/레이저 계열 보정
- `data-type="6"` 히든 보정
- 스프라이트 확대/위치 이동/오른쪽 마스킹 강화

## 결과
솔라/광자/히든 행성 썸네일 오른쪽의 미세한 옆 프레임 노출을 더 확실하게 줄인다.


---

# 추가 패치: Solar Thumbnail Centering Fix

## 목적
솔라 행성 썸네일에서 오른쪽 프레임 노출은 줄었지만, 행성 이미지가 왼쪽으로 잘려 보이는 문제를 수정했다.

## 변경 사항
- 솔라 행성 썸네일 영역만 소폭 확대
- 솔라 스프라이트 확대율을 완화
- background-position-x를 중앙 기준으로 재보정
- 과도한 오른쪽 마스킹 제거
- 솔라 카드 내부 간격 보정

## 결과
솔라 행성이 썸네일 중앙에 더 자연스럽게 배치되고 좌우 잘림이 완화된다.


---

# 추가 패치: Mobile Skill Modal Typography Fix

## 목적
랜덤 스킬 선택 UI에서 텍스트가 너무 커서 가독성이 떨어지는 문제를 개선했다.

## 변경 사항
- 랜덤 스킬 카드 전체 타이포 크기 축소
- 스킬명/효과명/LV/설명/하단 태그 크기 재정렬
- 카드 높이 축소
- 아이콘 크기 축소
- 한글 개행을 줄이기 위해 `word-break: keep-all` 유지
- 390px 이하 화면 추가 보정

## 결과
랜덤 스킬 UI가 덜 답답하고, 카드 안에서 텍스트 위계가 더 자연스럽게 보인다.


---

# 추가 패치: Photon / Hidden Thumbnail Center

## 목적
모바일 행성 구매 카드에서 광자 행성과 히든 스타 엔진 썸네일이 왼쪽으로 치우쳐 보이는 문제를 개별 보정했다.

## 변경 사항
- `data-type="5"` 광자 행성 썸네일을 오른쪽으로 이동
- `data-type="6"` 히든 스타 엔진 썸네일을 오른쪽으로 이동
- 두 타입만 별도 width/background-position 보정
- 솔라 및 다른 행성 썸네일은 유지

## 결과
광자/히든 스타 엔진 아이콘이 썸네일 박스 중앙에 더 가깝게 표시된다.


---

# 추가 패치: Photon / Hidden Hard Crop Final

## 목적
광자/히든 스타 엔진 썸네일에서 옆 스프라이트 프레임이 계속 보이는 문제를 근본적으로 수정했다.

## 변경 사항
- 기존 고정 CSS background-position 방식 제거
- `applyLiveThumb()`에서 현재 레벨/프레임 column 기준으로 위치를 동적 계산
- 광자(type 5), 히든(type 6)만 hard crop 처리
- 썸네일 마스크 폭 축소
- 양쪽 내부 마스킹 적용

## 결과
레벨이 바뀌어도 광자/히든 썸네일이 현재 프레임 기준으로 표시되고, 옆 프레임 노출이 크게 줄어든다.


---

# 추가 패치: Hangar Thumbnail PNG Assets

## 목적
하단 행성 구매 카드에서 스프라이트 시트 기반 썸네일이 옆 프레임을 노출하거나 중앙 정렬이 틀어지는 문제를 근본적으로 해결했다.

## 변경 사항
- 각 행성별/레벨별 하단 썸네일 PNG 생성
- `thumbs/{planet_id}_lv{level}.png` 경로 추가
- 총 7개 행성 × 5레벨 = 35개 썸네일 생성
- 하단 행성 카드에서는 스프라이트 대신 개별 PNG 사용
- `applyLiveThumb()`에서 하단 카드인지 판단 후 개별 썸네일 적용
- 전투 보드 렌더링은 기존 스프라이트 방식 유지

## 결과
하단 카드 썸네일에서 옆 프레임이 보이지 않고, 각 행성 이미지가 중앙에 안정적으로 표시된다.


---

# 추가 패치: Mobile Skill Card Vertical Layout

## 목적
랜덤 스킬 선택 카드가 모바일에서 좌우 배치로 어색하게 보이고, 텍스트와 아이콘 정렬이 불안정한 문제를 개선했다.

## 변경 사항
- 랜덤 스킬 카드를 세로형 모바일 카드로 재배치
- 구성 순서: 등급 → 유닛 타입 → 행성명 → 아이콘 → 스킬명/LV → 효과 설명 → 성장 조건
- 아이콘을 카드 중앙 상단에 배치
- 스킬명과 설명을 중앙 정렬
- 하단 성장 조건 태그를 카드 아래쪽에 고정
- 한글 개행 최소화 유지
- 390px 이하 화면 추가 보정

## 결과
랜덤 스킬 선택 UI가 모바일 카드처럼 안정적으로 정렬되고, 시선 흐름이 자연스러워진다.


---

# 추가 패치: Hangar Thumbnail PNG Level 1-6 Final

## 목적
하단 행성 카드에서 레벨이 올라갈 때 스프라이트 프레임 경계가 다시 보이는 문제를 CSS 보정이 아닌 이미지 자산 분리 방식으로 해결했다.

## 변경 사항
- 하단 썸네일 전용 PNG를 레벨 1~6까지 생성
- 7개 행성 × 6레벨 = 42개 PNG 추가
- 경로: `thumbs/{planet_id}_lv{level}.png`
- 스프라이트에서 가장 큰 알파 컴포넌트만 추출해 옆 프레임 노출 제거
- 하단 카드 `applyLiveThumb()`는 `thumbs` PNG를 직접 호출
- 레벨 6은 원본 스프라이트에 6번째 프레임이 없어 현재는 레벨 5 이미지를 기반으로 생성
- 전투 보드 렌더링은 기존 스프라이트 방식 유지

## 결과
하단 카드 썸네일은 레벨이 올라가도 옆 이미지가 섞이지 않고 안정적으로 중앙 표시된다.
