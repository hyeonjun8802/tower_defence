# Mobile UI Layout Patch

- Fixed mobile-web overlap/clipping by replacing fixed-height HUD behavior with stacked responsive layout.
- Board stays at full width with preserved aspect ratio.
- Hangar cards reduced and anchored cleanly inside the board.
- Side panels now reflow as a responsive grid on tablet/mobile and single column on very small screens.
- Offline progression panel gets its own scroll area instead of forcing the whole HUD to clip.
