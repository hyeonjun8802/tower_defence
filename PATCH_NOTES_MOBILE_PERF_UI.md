# Mobile Performance & Combat UI Patch

## 반영 내용

- BGM ON 상태에서 모바일 브라우저가 버벅일 수 있는 구간을 완화했습니다.
  - 모바일/저전력 환경에서는 반복 사격음, 피격음, 처치음 같은 고빈도 SFX를 자동으로 줄입니다.
  - BGM Audio 객체를 재사용하고, 불필요한 새 Audio 생성을 줄였습니다.
  - 타워 발사 합성음 호출 간격을 늘려 프레임 드랍 가능성을 줄였습니다.
- 전투 상단 알림을 정리했습니다.
  - 여러 알림이 동시에 겹치지 않도록 한 번에 하나만 표시합니다.
  - 보스 스킬/성역 효과는 짧은 한글 문구 중심으로 표시합니다.
  - 긴 영어 스킬명은 전투 중 상단 알림에서 제거했습니다.
- 하단 전투 HUD를 모바일 기준으로 정리했습니다.
  - 스테이지 번호가 `1-10`에서 줄바꿈되지 않도록 non-breaking hyphen과 CSS nowrap을 적용했습니다.
  - 성역명은 한글 중심으로 줄이고, 작은 화면에서는 글자 크기를 낮췄습니다.
  - `GLOBAL` 표기를 `전역`으로 변경했습니다.

## 검증

- JavaScript syntax check 통과
- ZIP 압축 무결성 검사 통과
