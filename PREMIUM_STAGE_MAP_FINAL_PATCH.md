# Premium Stage Map Final Patch

## Applied
- Stage backgrounds are now rendered as full fixed backdrops inside the stage map.
- Each backdrop uses `cover` behavior with `background-position: left top`, so the art is never squashed; right-side cropping is allowed.
- Rebuilt the stage-node flow into a smoother premium curve instead of angular ㄹ-corners.
- Forced all 7 stage nodes, including stage 6 and 7, to use the planet-image assets directly.

## Intent
A premium mobile game style flow map with generous spacing, no route crossing, and full-screen stage mood art.
