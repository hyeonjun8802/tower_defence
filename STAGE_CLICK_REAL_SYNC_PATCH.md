# V12 Stage Click Real Sync Patch

## 문제
스테이지 노드를 클릭해도 하단 정보 패널 일부가 이전 선택값을 유지했습니다.
특히 `1장. 공허 성역`, 전역 효과/조각/숙련도 영역이 실제 선택 성역과 따로 움직였습니다.

## 원인
이전 하드 싱크 패치가 메인 게임 IIFE 밖에 배치되어, 내부 함수/상태(`renderStageMap`, `StageMapState`, `OFFLINE_CHAPTERS` 등)에 접근할 수 없는 구조였습니다.
문법 검사는 통과하지만 브라우저 런타임에서 동기화 코드가 정상 실행되지 않을 수 있었습니다.

## 수정
- 스테이지 클릭 싱크 코드를 메인 게임 스코프 내부로 통합했습니다.
- 스테이지 노드 클릭 시 다음 항목을 모두 선택 성역 기준으로 갱신합니다.
  - 배경 선택 상태
  - 상단 선택 성역 텍스트
  - 하단 제목/위험도/설명/태그
  - 중간 보스/최종 보스
  - 장별 시나리오
  - 전역 효과/조각/클리어/최고 웨이브
  - 숙련도 목표
  - ENTER / LOCKED 버튼 상태
- 잠긴 성역도 클릭 시 하단 정보는 미리보기로 표시합니다.
- 기존 외부 V11 동기화 블록은 제거했습니다.
- 하단 설명이 말줄임으로 잘리지 않도록 일부 CSS를 보강했습니다.

## 검증
- JS 문법 검사 통과
- ZIP 무결성 검사 통과
