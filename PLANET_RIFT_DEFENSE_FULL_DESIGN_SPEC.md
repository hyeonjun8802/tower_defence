# Planet Rift Defense: Cosmic Plates — 전체 시나리오 / 컨셉 / 시스템 기획서

> 기준 소스: `planet_rift_defense_v21_balance_tuned/index.html`  
> 문서 목적: 현재 HTML에 구현된 게임의 세계관, 시나리오, 플레이 구조, 행성 유닛, 장판, 랜덤 스킬, 히든 행성, 몬스터, 스테이지 효과, UI, 조작, 리소스 구성을 빠짐없이 정리한다.  
> 작성 기준: 이 문서는 현재 HTML 구현을 기준으로 정리한 설계 명세이며, 일부 세계관 문장은 HTML의 기능/명칭을 바탕으로 자연스럽게 보강한 설명이다.

---

## 1. 게임 한 줄 컨셉

**우주 균열이 열린 미래 성역에서, 행성 병기를 장판 위에 배치하고 병합하며, 레벨업마다 맵 위 아군 행성 1개만 무작위로 성장시키는 장판 중심 행성 디펜스 게임.**

핵심은 단순 타워 디펜스가 아니라 다음 세 가지가 결합된 구조다.

1. **행성 병기 디펜스**
   - 모든 아군 유닛은 행성 또는 행성형 전투 코어다.
   - 행성은 랜덤으로 소환되고, 같은 종류/레벨끼리 병합된다.

2. **장판 디펜스**
   - 일반 칸에 배치하면 출력 손실이 발생한다.
   - 증폭성운, 가속궤도, 중력렌즈, 불안정균열 같은 장판에 배치해야 제대로 방어할 수 있다.
   - 현재 밸런스는 1-1부터 장판의 필요성이 체감되도록 조정되어 있다.

3. **랜덤 유닛 성장**
   - 몬스터 처치로 EXP를 얻고 레벨업한다.
   - 레벨업 시 스킬 선택지가 뜨지만, 선택한 효과는 모든 행성에 적용되는 전역 강화가 아니다.
   - 맵 위 아군 행성 중 **1개가 무작위로 지정**되고, 그 행성만 성장한다.

---

## 2. 세계관 / 시나리오

### 2.1 배경

먼 미래, 인류는 행성을 무기화하는 기술을 완성했다.  
각 행성은 단순한 천체가 아니라, 고에너지 코어와 궤도 링, 소행성 위성 장치가 결합된 **전투용 행성 병기**다.

그러나 우주 곳곳에 `Rift`, 즉 **차원 균열**이 발생하면서 공허 생명체와 균열 몬스터들이 핵심 코어를 향해 침공하기 시작한다.  
플레이어는 전술 지휘관이 되어 여러 성역을 순회하며 코어를 방어해야 한다.

### 2.2 플레이어의 역할

플레이어는 직접 포탑을 조준하는 조종사가 아니라, 다음을 관리하는 **성역 방어 지휘관**이다.

- 어느 장판에 행성을 배치할지 결정
- 랜덤으로 소환된 행성을 병합
- 레벨업 시 어떤 행성 하나를 성장시킬지 선택
- 전술판 궁극기를 언제 사용할지 판단
- 코어 HP와 수정, EXP를 관리
- 히든 스타 엔진 융합 조건을 달성

### 2.3 적의 목적

적들은 균열항로를 따라 코어까지 접근한다.  
코어에 도달하면 코어 HP가 감소한다.  
보스는 더 큰 피해를 주며, 코어가 파괴되면 게임은 정지된다.

### 2.4 전체 진행감

게임은 `성역` 단위로 진행된다.  
각 성역은 10개 웨이브를 기준으로 순환하고, 10웨이브 이후 다음 성역 테마로 넘어간다.

성역은 다음 네 가지다.

| 순서 | 영문명 | 한글명 | 핵심 분위기 |
|---:|---|---|---|
| 1 | COSMIC VOID | 공허 성역 | 기본 우주 균열, 오로라, 공허 에너지 |
| 2 | FROST EXPANSE | 빙결 외곽 | 냉기, 눈보라, 빙결 |
| 3 | LAVA NEBULA | 용암 성운 | 태양 플레어, 열기, 적 방어 상승 |
| 4 | JUNGLE CORE | 생체 정글 | 생체 포자, 독성, 공격속도 저하 |

---

## 3. 화면 / UI 구성

### 3.1 메인 캔버스

| 항목 | 값 |
|---|---|
| 메인 게임 캔버스 | `760 x 720` |
| 그리드 | `8 x 8` |
| 셀 크기 | `56` |
| 코어 위치 | 우측 하단 근처, `CORE = {x: W - 74, y: H - 95}` |
| 배경 | 스테이지별 우주 배경 이미지 |
| 별 레이어 | 캔버스 내부 별 파티클 + 전체 화면 starfield |

### 3.2 우측 패널

우측 패널은 다음 정보를 표시한다.

| 패널 | 표시 내용 |
|---|---|
| Stage Header | 현재 성역명, 스테이지 라벨, 웨이브 타입, 웨이브 진행바 |
| Stat Cards | Crystal, Core HP, EXP, Level |
| Command Buttons | 랜덤 소환, 자동 병합, 속도, 일시정지, BGM |
| Tactical Plates | 전술판 3종: 균열 포획, 궤도 포격, 코어 과충전 |
| Command Node | 선택한 행성 또는 장판 정보 |
| Battle Log | 최근 로그 최대 6개 |

### 3.3 하단 행성 카드

하단 카드는 더 이상 클릭 업그레이드 버튼이 아니다.  
현재 구현 기준 역할은 **행성 계열 정보 표시용 ROSTER/INFO 카드**다.

- 6개 기본 행성 + 1개 히든 행성 표시
- 히든 행성은 해금 전 `LOCK`
- 카드 클릭 강화 기능은 제거됨
- 반응형에서는 가로 스크롤형 UI로 동작

### 3.4 스킬 선택 모달

레벨업 또는 보물성 보상에서 랜덤 성장 선택지가 뜬다.

- 3개 선택지 제공
- 각 선택지는 특정 아군 행성 1개와 그 행성의 스킬 1개로 구성
- 선택하면 해당 행성 1개만 성장
- 희귀 스킬 선택일 경우 같은 스킬이 2회 적용됨

---

## 4. 기본 리소스 / 수치

### 4.1 게임 시작 상태

| 항목 | 값 |
|---|---:|
| 시작 성역 | 공허 성역 |
| 시작 웨이브 | 1 |
| 시작 수정 | 420 |
| 코어 HP | 22 / 22 |
| 시작 EXP | 0 |
| 시작 레벨 | 1 |
| 다음 레벨 필요 EXP | 140 |
| 시작 속도 | 1x |
| 랜덤 소환 비용 | 100 |
| 히든 행성 상태 | 잠금 |
| BGM | 버튼 클릭 후 활성화 |

### 4.2 속도

게임 속도 버튼은 다음 순서로 순환한다.

```text
1x → 2x → 3x → 1x
```

### 4.3 웨이브 진행

- 웨이브를 클리어하면 수정 보상을 받는다.
- 웨이브가 10을 초과하면 웨이브는 1로 돌아가고 다음 성역으로 넘어간다.
- 현재 HTML 기준 실제 보스 추가 로직은 `S.wave % 4 === 0`일 때 보스를 추가한다.
- UI의 웨이브 타입 표기는 `S.wave % 5 === 0`일 때 `보스 웨이브`로 표시된다.
  - 즉, 현재 구현에는 **실제 보스 발생 조건과 UI 보스 표시 조건이 서로 다르다.**
  - 추후 정리하려면 둘 중 하나로 통일하는 것이 좋다.

---

## 5. 조작법

| 입력 | 동작 |
|---|---|
| 마우스/터치 드래그 | 행성 이동 |
| 같은 타입/같은 레벨 위로 드래그 | 병합 |
| 빈 장판 클릭 | 장판 정보 표시 |
| 행성 클릭 | 행성 정보 표시 |
| `1` | 랜덤 소환 |
| `2` | 속도 변경 |
| `M` | 자동 병합 |
| `Space` | 일시정지 / 재개 |
| `Q` | 균열 포획 |
| `W` | 궤도 포격 |
| `E` | 코어 과충전 |
| BGM 버튼 | BGM ON/OFF |

---

## 6. 핵심 플레이 루프

```text
1. 웨이브 시작
2. 장판 위치 확인
3. 랜덤 행성 소환
4. 장판 보너스를 고려해 행성 배치
5. 같은 타입/레벨 행성 병합
6. 적 처치로 낮은 EXP와 낮은 수정 획득
7. EXP가 차면 레벨업
8. 랜덤 유닛 성장 선택
9. 특정 아군 행성 1개만 강화
10. 웨이브 클리어 보상 획득
11. 다음 웨이브/다음 성역 진행
12. 기본 행성 6종 Lv.5 달성 시 히든 스타 엔진 융합
```

---

## 7. 장판 시스템

장판은 게임의 핵심이다.  
현재 밸런스에서는 일반 공허판에 행성을 배치하면 출력 손실이 발생한다.  
장판 없이 진행하면 초반부터 방어가 어렵게 설계되어 있다.

### 7.1 장판 종류

| 코드 | 이름 | 설명 | 실제 효과 |
|---|---|---|---|
| `empty` | 공허판 | 출력 손실: 피해 -38%, 공속 저하 | 피해 배율 `0.72`, 쿨다운 배율 `1.16`, 사거리 `-3` |
| `path` | 균열항로 | 적 이동 경로, 배치 불가 | 배치 불가 |
| `blocked` | 운석폐허 | 배치 불가 | 배치 불가 |
| `amp` | 증폭성운 | 핵심 장판: 피해 +65% | 피해 배율 `1.55`, 쿨다운 `0.96` |
| `coil` | 가속궤도 | 핵심 장판: 공격속도 대폭 증가 | 피해 배율 `1.02`, 쿨다운 배율 `0.62`, 사거리 `+4` |
| `lens` | 중력렌즈 | 핵심 장판: 사거리 +50 | 피해 배율 `0.92`, 쿨다운 `0.96`, 사거리 `+50` |
| `mine` | 수정광맥 | 보상 장판: 소량 추가 보상 | 피해 배율 `0.82`, 쿨다운 `1.08`, 보상 보정값 `+1` |
| `rift` | 불안정균열 | 위험 장판: 피해 +95%, 과열 | 피해 배율 `1.82`, 쿨다운 `1.10`, 사거리 `+8`, 과열 발생 |

### 7.2 장판 설계 의도

| 장판 | 의도 |
|---|---|
| 공허판 | 임시 배치용. 장기 방어에는 부적합 |
| 증폭성운 | 고화력 행성 배치용 |
| 가속궤도 | 연쇄/빙결/독성처럼 공격 빈도가 중요한 행성용 |
| 중력렌즈 | 광자/프로스트처럼 사거리 가치가 높은 행성용 |
| 수정광맥 | 경제 보조용. 화력은 낮음 |
| 불안정균열 | 고위험 고화력 장판. 과열 리스크 존재 |

### 7.3 장판 생성

- 웨이브마다 경로가 생성된다.
- 경로 주변 셀은 `path`로 지정되어 배치 불가가 된다.
- 일부 셀은 `blocked`로 지정된다.
- 이후 `amp`, `coil`, `lens`, `mine`, `rift` 등이 무작위로 배치된다.
- 맵 구조가 바뀌면서 기존 행성이 배치 불가 위치에 있으면 근처 가능한 칸으로 자동 재배치된다.

---

## 8. 아군 행성 유닛

### 8.1 기본 행성 6종

| 타입 | 이름 | 색상 | 사거리 | 공격력 | 쿨다운 | 공격 방식 | 카드 설명 | 태그 |
|---|---|---|---:|---:|---:|---|---|---|
| `solar` | 솔라 행성 | 주황 | 132 | 48 | 34 | `splash` | 광역 폭발 · 연소 | 광역, 연소, 합류점 |
| `frost` | 프로스트 행성 | 하늘색 | 145 | 30 | 30 | `slow` | 빙결 감속 · 제어 | 감속, 빙결, 제어 |
| `storm` | 스톰 행성 | 노랑 | 128 | 36 | 32 | `chain` | 연쇄 번개 · 장갑 파괴 | 연쇄, 장갑파괴, 다중경로 |
| `toxic` | 바이오 행성 | 초록 | 126 | 24 | 27 | `poison` | 독성 침식 · 지속 피해 | 독, 지속피해, 보스 |
| `void` | 블랙홀 행성 | 보라 | 122 | 22 | 48 | `gravity` | 소형 블랙홀 · 구속 | 끌어당김, 디버프, 군중제어 |
| `laser` | 광자 행성 | 연보라 | 165 | 58 | 44 | `beam` | 고출력 광자 빔 | 관통, 저격, 직선 |

### 8.2 히든 행성

| 타입 | 이름 | 사거리 | 공격력 | 쿨다운 | 공격 방식 | 특징 |
|---|---|---:|---:|---:|---|---|
| `starengine` | 히든 스타 엔진 | 202 | 148 | 24 | `crit` | 치명타, 융합, 최종병기 |

#### 히든 스타 엔진 기본 능력

| 항목 | 값 |
|---|---:|
| 치명타 확률 | `0.48` |
| 치명타 배율 | `2.7` |
| STAR BURST 폭발 확률 | `0.12` |
| STAR BURST 폭발 반경 | `96` |

#### 히든 행성 해금 조건

기본 행성 6종을 각각 하나 이상 `Lv.5 이상`으로 보유하면 자동으로 융합된다.

```text
솔라 Lv.5+
프로스트 Lv.5+
스톰 Lv.5+
바이오 Lv.5+
블랙홀 Lv.5+
광자 Lv.5+
→ 히든 스타 엔진 생성
```

융합 시 처리:

- 조건에 맞는 6개 행성을 찾음
- 6개 행성의 평균 위치에 가장 가까운 행성을 기준점으로 선택
- 기준점 외 5개 행성 제거
- 기준점에 히든 스타 엔진 Lv.1 생성
- 하단 히든 카드 잠금 해제
- 이펙트, 링, 로그 출력

---

## 9. 행성 배치 / 병합

### 9.1 소환

현재 소환은 **랜덤 소환만 가능**하다.

| 항목 | 값 |
|---|---:|
| 소환 비용 | 수정 100 |
| 소환 대상 | 기본 행성 6종 중 1개 |
| 소환 위치 | 배치 가능한 빈 칸 중 랜덤 |
| 히든 행성 | 직접 소환 불가 |

### 9.2 이동

- 행성을 드래그해 다른 배치 가능 칸으로 이동할 수 있다.
- 이동 불가 위치에 놓으면 원래 위치로 돌아간다.

### 9.3 병합

같은 타입, 같은 레벨 행성을 겹치면 병합된다.

```text
같은 행성 Lv.N + 같은 행성 Lv.N → 같은 행성 Lv.N+1
```

조건:

- 타입이 같아야 함
- 레벨이 같아야 함
- 레벨은 12 미만이어야 함

병합 시 성장 정보 처리:

- 두 행성의 스킬 강화값을 합산한 뒤 `75%` 비율로 보존
- 두 행성의 스킬 레벨 정보는 같은 스킬 기준 더 높은 레벨을 유지
- 새 행성은 새 UID를 가진다

### 9.4 자동 병합

`자동 병합` 버튼 또는 `M` 키로 실행한다.

- 맵 전체를 순회하며 병합 가능한 첫 쌍을 찾음
- 병합 후 80ms 뒤 재귀적으로 다시 시도
- 병합 가능한 행성이 없으면 토스트 표시

---

## 10. 행성 공격 시스템

### 10.1 공통 스탯 계산

행성의 실제 전투 스탯은 다음 요소가 합산된다.

```text
기본 행성 스탯
+ 레벨 보정
+ 장판 보정
+ 개별 랜덤 스킬 성장 보정
+ 전술판 일시 버프
```

#### 공격력

```text
(기본 공격력 + 레벨 * 15) * 장판 피해 배율 * (1 + 개별 damage 성장)
```

#### 사거리

```text
max(70, 기본 사거리 + 레벨 * 8 + 장판 사거리 + 개별 range 성장)
```

#### 쿨다운

```text
max(8, (기본 쿨다운 - 레벨 * 1.7) * 장판 쿨다운 배율 * 스테이지 페널티 / (1 + 개별 fireRate + 전술 부스트))
```

### 10.2 타겟팅

- 사거리 내 적을 찾는다.
- 가장 많이 진행한 적, 즉 코어에 가장 가까운 적을 우선 공격한다.

### 10.3 행성별 공격 방식

#### 솔라 행성

- 투사체 적중 시 광역 피해
- 연소 DOT 부여
- `solar_flare` 성장 시 폭발 반경 증가
- `solar_burn` 성장 시 연소 지속피해 증가

#### 프로스트 행성

- 피해 + 감속
- 확률적으로 빙결
- `frost_lock` 성장 시 빙결 확률/지속 증가
- `frost_range` 성장 시 사거리 증가

#### 스톰 행성

- 연쇄 공격
- 연쇄 대상 수는 레벨과 `storm_chain` 성장에 따라 증가
- `storm_break` 성장 시 표식 피해 증폭

#### 바이오 행성

- 독성 피해
- 독 DOT 부여
- `toxic_slow` 성장 시 중독 대상 둔화
- 보스/고체력 적에게 지속 피해로 대응

#### 블랙홀 행성

- 적 근처에 소형 블랙홀 이상현상 생성
- 블랙홀은 반경 내 적을 끌어당기고 감속/피해/표식을 부여
- `void_pull`, `void_radius`, `void_decay` 성장에 따라 흡입력, 반경, 피해 강화

#### 광자 행성

- 직선 관통 빔
- 빔 경로에 가까운 적에게 피해
- `laser_width` 성장 시 빔 폭 증가
- `laser_cooler` 성장 시 공격속도 증가

#### 히든 스타 엔진

- 높은 치명타 확률과 치명타 배율 보유
- 적중 시 치명타 발생 가능
- 낮은 확률로 `STAR BURST` 발생
- STAR BURST는 주변 적에게 `x3` 폭발 피해
- 히든 전용 성장으로 치명타 확률, 폭발 확률, 치명타 배율 강화 가능

---

## 11. 랜덤 유닛 성장 / 스킬 트리

### 11.1 레벨업 구조

몬스터 처치로 EXP를 얻는다.  
EXP가 다음 요구량 이상이면 레벨업한다.

```text
레벨업 시:
1. 현재 EXP에서 필요 EXP 차감
2. 레벨 +1
3. 다음 필요 EXP = floor(이전 필요 EXP * 1.24 + 24)
4. 랜덤 유닛 성장 선택권 +1
5. 스킬 선택 모달 표시
```

### 11.2 중요한 설계 원칙

현재 스킬은 전역 버프가 아니다.

```text
스킬 선택 → 맵 위 아군 행성 중 1개가 지정됨 → 해당 행성 하나만 성장
```

이 구조는 다음 의도를 가진다.

- 특정 행성 하나만 강해지므로 난이도가 유지됨
- 랜덤성이 높아져 매 판 다른 빌드가 형성됨
- 장판 배치와 병합 판단이 더 중요해짐
- 모든 행성이 균등하게 강해지는 쉬운 구조를 방지

### 11.3 스킬 선택지 생성

스킬 선택 모달은 3개 선택지를 만든다.

각 선택지는 다음 정보를 가진다.

| 항목 | 의미 |
|---|---|
| 대상 행성 | 맵 위 아군 행성 중 랜덤 1개 |
| 행성 타입 | 대상 행성의 타입 |
| 스킬 | 해당 타입의 스킬 트리에서 랜덤 1개 |
| 다음 레벨 | 해당 행성이 가진 해당 스킬 레벨 +1 |

### 11.4 희귀 스킬

황금 보물성 보상으로 희귀 스킬 선택이 뜰 수 있다.  
희귀 스킬은 선택 시 같은 스킬 효과가 2회 적용된다.

### 11.5 스킬 트리 전체

#### 솔라 행성 스킬

| ID | 이름 | 효과 |
|---|---|---|
| `solar_core` | 태양 핵융합로 | 해당 솔라 행성 공격력 `+18% * Lv` |
| `solar_flare` | 플레어 장판 | 폭발 반경 `+22% * Lv` |
| `solar_burn` | 코로나 연소 | 연소 지속피해 `+35% * Lv` |

#### 프로스트 행성 스킬

| ID | 이름 | 효과 |
|---|---|---|
| `frost_core` | 극저온 냉각핵 | 해당 프로스트 행성 공격력 `+16% * Lv` |
| `frost_lock` | 절대영도 잠금 | 빙결 확률/지속 `+20% * Lv`, 빙결 확률 추가 `+5%`씩 |
| `frost_range` | 냉기 렌즈 | 사거리 `+10 * Lv` |

#### 스톰 행성 스킬

| ID | 이름 | 효과 |
|---|---|---|
| `storm_coil` | 플라즈마 유도 코일 | 해당 스톰 행성 공격력 `+15% * Lv` |
| `storm_chain` | 연쇄 회로 증폭 | 연쇄 대상 `+1 * Lv` |
| `storm_break` | 전하 표식 | 표식 피해 증폭 `+8% * Lv` |

#### 바이오 행성 스킬

| ID | 이름 | 효과 |
|---|---|---|
| `toxic_reactor` | 생체화학 반응로 | 독성 지속피해 `+40% * Lv` |
| `toxic_spore` | 포자 확산막 | 독성 공격력 `+16% * Lv` |
| `toxic_slow` | 마비 독성 | 중독 대상 둔화 강화 `+1 * Lv` |

#### 블랙홀 행성 스킬

| ID | 이름 | 효과 |
|---|---|---|
| `void_pull` | 중력 우물 심화 | 블랙홀 흡입력 `+25% * Lv` |
| `void_radius` | 사건지평 확장 | 블랙홀 반경 `+18% * Lv` |
| `void_decay` | 공간 붕괴파 | 블랙홀 피해 `+17% * Lv` |

#### 광자 행성 스킬

| ID | 이름 | 효과 |
|---|---|---|
| `laser_lens` | 광자 렌즈 정렬 | 광자 행성 공격력 `+17% * Lv` |
| `laser_width` | 관통 빔 확장 | 빔 폭 `+5 * Lv` |
| `laser_cooler` | 광자 냉각 채널 | 공격속도 `+12% * Lv` |

#### 히든 스타 엔진 스킬

| ID | 이름 | 효과 |
|---|---|---|
| `star_crit` | 쌍성 임계점 | 치명타 확률 `+6% * Lv` |
| `star_burst` | STAR BURST 증폭 | 폭발 확률 `+4% * Lv` |
| `star_nova` | 백색왜성 방출 | 치명타 배율 `+25% * Lv` |

---

## 12. 전술판 궁극기

우측 패널의 `TACTICAL PLATES`에는 3개 전술기가 있다.  
각 전술기는 EXP를 소모하며, 쿨다운이 있다.

| 키 | 이름 | EXP 비용 | 쿨다운 기준값 | 효과 |
|---|---|---:|---:|---|
| Q | 균열 포획 | 70 | 720 | 선두 적 중심에 강력한 균열/블랙홀 생성, 적 끌어당김, 감속, 표식, 피해 |
| W | 궤도 포격 | 75 | 820 | 진행도가 높은 적 최대 5명을 순차 포격, 범위 피해 |
| E | 코어 과충전 | 65 | 900 | 코어 HP +3, 모든 행성 주변 이펙트, 일시 공격속도 증가 |

### 12.1 균열 포획

- 가장 많이 진행한 적을 기준으로 발동
- 중심 좌표: 선두 적 위치
- 반경: 230
- 즉시 효과:
  - 중심으로 적 위치 일부 이동
  - 감속 210
  - 표식 240
  - 피해 180
- 추가로 블랙홀 이상현상 생성:
  - 반경 210
  - 피해 28
  - 흡입력 2.1
  - 지속 130

### 12.2 궤도 포격

- 진행도 높은 적 최대 5명을 대상으로 함
- 각 대상에 90ms 간격으로 포격
- 포격 범위: 86
- 피해: `230 + 현재 웨이브 * 12`
- 플래시/흔들림 이펙트 발생

### 12.3 코어 과충전

- 코어 HP 3 회복
- 전술 부스트 `0.28` 부여
- 전술 부스트는 시간이 지나며 감소
- 행성 공격속도를 일시적으로 증가시키는 역할

---

## 13. 적 / 몬스터

### 13.1 적 종류

| 타입 | HP 계수 | 속도 | 크기 | 보상 계수 | EXP | 특수 |
|---|---:|---:|---:|---:|---:|---|
| `grunt` | 1.62 | 1.08 | 13 | 0.50 | 3 | 기본형 |
| `runner` | 1.05 | 1.82 | 11 | 0.50 | 3 | 빠른 이동 |
| `brute` | 4.10 | 0.84 | 18 | 0.90 | 5 | 방어력 0.23 |
| `regen` | 2.65 | 0.98 | 15 | 0.80 | 5 | 재생 |
| `treasure` | 3.75 | 1.00 | 18 | 2.20 | 8 | 황금 보물성 |
| `boss` | 20.00 | 0.64 | 30 | 3.50 | 18 | 방어력 0.31, 코어 도달 피해 증가 |

### 13.2 실제 HP 계산

```text
maxHp = floor(295 * 타입 HP 계수 * (1 + wave * 0.30 + theme * 0.38))
```

### 13.3 실제 속도 계산

```text
speed = 타입 속도 * (1 + wave * 0.032 + theme * 0.028)
```

상태이상 보정:

- 감속 중이면 속도 `* 0.55`
- 빙결 중이면 속도 `* 0.12`

### 13.4 재생형 적

재생형 적은 매 프레임 HP를 회복한다.

```text
hp += 0.18 * dt * (1 + wave * 0.045)
```

### 13.5 피해 계산

적이 받는 피해는 방어력과 스테이지 방어 보정 영향을 받는다.

```text
finalDamage = damage * (1 - clamp(enemyArmor + stageArmorBonus, 0, 0.72))
```

표식 상태면 피해가 증가한다.

```text
if mark > 0:
  finalDamage *= 1.22
```

### 13.6 처치 보상

처치 시 수정 보상:

```text
reward = max(1, floor((6 + wave * 0.9) * enemyReward * (1 + rewardMod)))
```

EXP는 적 타입별 고정값을 지급한다.  
현재 밸런스는 게임을 어렵게 만들기 위해 EXP와 보상을 낮춘 상태다.

### 13.7 황금 보물성

보물성 처치 시:

- 55% 확률: 수정 보너스 지급
  - 보너스: `45 + wave * 8`
- 45% 확률: 희귀 스킬 선택 모달
  - 희귀 스킬은 선택한 유닛 스킬을 2회 적용

---

## 14. 웨이브 / 스폰

### 14.1 웨이브별 적 수

```text
count = 28 + wave * 6 + theme * 6
```

### 14.2 적 타입 배치 규칙

웨이브 큐 생성 시 다음 규칙을 적용한다.

| 조건 | 타입 |
|---|---|
| 기본값 | grunt |
| `i % 4 === 1` | runner |
| `i % 6 === 3` | brute |
| `wave >= 2 && i % 8 === 5` | regen |
| `wave >= 3 && i % 5 === 0` | brute |
| `wave >= 6 && i % 7 === 2` | regen |
| 랜덤 확률 `0.02 + treasureMod` | treasure |
| `wave % 4 === 0` | boss 1체 추가 |

### 14.3 스폰 간격

루프에서의 스폰 간격:

```text
interval = max(18, 54 - wave * 1.8 - theme * 3)
```

웨이브 시작 시 초기 스폰 타이머:

```text
spawnTimer = 13
```

---

## 15. 스테이지 / 성역 효과

각 성역은 진입 시, 그리고 전투 중 일정 시간이 지나면 전체 맵 이펙트와 함께 스테이지 효과를 발생시킨다.

### 15.1 공허 성역 — AURORA SHIFT

| 항목 | 내용 |
|---|---|
| 효과명 | 오로라 균열 |
| 연출 | 전장 전체 흔들림, 오로라 계열 FX |
| 실제 효과 | 현재 적 전체에 표식 부여 |
| 로그 | `스테이지 효과: 오로라 균열` |

### 15.2 빙결 외곽 — FROST BLIZZARD

| 항목 | 내용 |
|---|---|
| 효과명 | 눈보라 |
| 연출 | 눈보라 FX |
| 실제 효과 | 일부 아군 행성이 잠시 빙결되어 공격 정지 |
| 선택 수 | 최소 1개, 최대 3개, 전체 행성의 약 22% |
| 로그 | `스테이지 효과: 눈보라 / 일부 행성 공격 정지` |

### 15.3 용암 성운 — SOLAR FLARE

| 항목 | 내용 |
|---|---|
| 효과명 | 태양 플레어 |
| 연출 | 플레어 FX |
| 실제 효과 | 적 방어력 상승 |
| 수치 | `stageArmorBonus = 0.18` |
| 지속 | 약 5.2초 |
| 로그 | `스테이지 효과: 플레어 / 적 방어력 상승` |

### 15.4 생체 정글 — BIO SPORE FIELD

| 항목 | 내용 |
|---|---|
| 효과명 | 생체 포자장 |
| 연출 | 포자장 FX |
| 실제 효과 | 행성 공격속도 저하 |
| 수치 | `globalCooldownPenalty = 0.28` |
| 지속 | 약 5.6초 |
| 로그 | `스테이지 효과: 포자장 / 공격속도 감소` |

---

## 16. 경제 / 성장 밸런스

### 16.1 현재 밸런스 의도

현재 버전은 쉬운 게임이 아니라, 초반부터 장판을 활용해야 하는 난이도를 목표로 한다.

- 일반 공허판 배치는 화력 손실
- 몬스터는 1-1부터 비교적 강함
- 처치 EXP는 낮음
- 처치 수정 보상도 낮음
- 소환 비용은 100
- 레벨업은 빠르게 반복되지 않음
- 스킬은 전역 강화가 아니라 단일 유닛 강화

### 16.2 웨이브 클리어 보상

```text
bonus = 150 + wave * 24 + theme * 55
```

웨이브 클리어 시:

- 보너스 수정 지급
- 코어 HP는 `repair` 모드만큼 회복
- 다음 웨이브 진행

현재 코드상 `repair` 모드는 전역 스킬 구조에서 남아 있으나, v17 이후 랜덤 유닛 스킬 트리 중심으로 바뀌면서 일반 레벨업 스킬에서는 직접 증가하지 않는다.

---

## 17. 그래픽 / 리소스

### 17.1 배경 이미지

| 파일 | 용도 |
|---|---|
| `space_bg.png` | 공허 성역 배경 |
| `bg_ice.png` | 빙결 외곽 배경 |
| `bg_lava.png` | 용암 성운 배경 |
| `bg_jungle.png` | 생체 정글 배경 |

### 17.2 스테이지 FX 이미지

| 파일 | 용도 |
|---|---|
| `fx_cosmic.png` | 공허 성역 전체 맵 FX |
| `fx_ice.png` | 빙결 외곽 눈보라 FX |
| `fx_lava.png` | 용암 성운 플레어 FX |
| `fx_jungle.png` | 생체 정글 포자장 FX |

### 17.3 행성 이미지

| 파일 | 용도 |
|---|---|
| `planet_units_planet_style_v15.png` | 6개 기본 행성 + 1개 히든 행성을 포함한 단일 스프라이트 시트 |

행성 이미지는 단일 PNG 시트에서 타입별 위치를 잘라 사용한다.

| 설정 | 값 |
|---|---:|
| 열 | 4 |
| 행 | 2 |
| 기본 행성 수 | 6 |
| 히든 타입 인덱스 | 6 |
| 기본 렌더 크기 | 49 |
| 렌더 스케일 | 0.98 |

### 17.4 행성 주변 소행성 / 위성 연출

각 행성 주변에는 작은 소행성 또는 위성이 궤도를 따라 움직인다.

- 행성 본체는 단일 PNG로 정적 표시
- 주변 궤도 링과 소행성은 캔버스에서 절차적으로 렌더링
- 소행성은 궤도 회전 + 위아래 미세 흔들림 적용
- 히든 행성은 소행성 개수가 더 많고 링이 더 강조됨

### 17.5 별 배경

별 배경은 두 계층으로 나뉜다.

1. 전체 화면 starfield
   - 3개 레이어
   - 각 레이어는 다른 속도와 알파값
2. 게임판 내부 fieldStars
   - 캔버스 내부에 별이 흘러가는 느낌
   - 배경 이미지 위에 얇은 우주 안개 레이어와 함께 표시

---

## 18. 사운드

### 18.1 BGM

| 파일 | 용도 |
|---|---|
| `audio/bgm_cosmic.wav` | 공허 성역 BGM |
| `audio/bgm_ice.wav` | 빙결 외곽 BGM |
| `audio/bgm_lava.wav` | 용암 성운 BGM |
| `audio/bgm_jungle.wav` | 생체 정글 BGM |

BGM은 브라우저 자동재생 정책 때문에 사용자가 BGM 버튼을 누른 뒤 재생된다.

- 루프 재생
- 볼륨 `0.34`
- 스테이지 변경 시 해당 테마 BGM으로 전환

### 18.2 SFX

| 파일 | 용도 |
|---|---|
| `audio/sfx_shot.wav` | 일반 발사 |
| `audio/sfx_beam.wav` | 광자 빔 |
| `audio/sfx_blackhole.wav` | 블랙홀 / 균열 포획 |
| `audio/sfx_nova.wav` | 궤도 포격 / 초신성 계열 |
| `audio/sfx_hit.wav` | 코어 피격 |
| `audio/sfx_kill.wav` | 적 처치 |
| `audio/sfx_treasure.wav` | 보물성 보상 |
| `audio/sfx_level.wav` | 레벨업 / 코어 과충전 |
| `audio/sfx_summon.wav` | 행성 소환 |
| `audio/sfx_merge.wav` | 행성 병합 |

특정 효과음 볼륨:

| 효과음 | 볼륨 |
|---|---:|
| blackhole | 0.55 |
| nova | 0.58 |
| treasure | 0.56 |
| level | 0.48 |
| hit | 0.42 |
| 그 외 | 0.36 |

---

## 19. 파일 구성

현재 패키지 기준 주요 파일은 다음과 같다.

```text
planet_rift_defense_v21/
├── index.html
├── README.md
├── DESIGN_SPEC.md
├── AUDIO_SOURCE_RESEARCH.md
├── run_localhost.sh
├── run_localhost.bat
├── space_bg.png
├── bg_ice.png
├── bg_lava.png
├── bg_jungle.png
├── fx_cosmic.png
├── fx_ice.png
├── fx_lava.png
├── fx_jungle.png
├── planet_units_planet_style_v15.png
└── audio/
    ├── bgm_cosmic.wav
    ├── bgm_ice.wav
    ├── bgm_lava.wav
    ├── bgm_jungle.wav
    ├── sfx_shot.wav
    ├── sfx_beam.wav
    ├── sfx_blackhole.wav
    ├── sfx_nova.wav
    ├── sfx_hit.wav
    ├── sfx_kill.wav
    ├── sfx_treasure.wav
    ├── sfx_level.wav
    ├── sfx_summon.wav
    └── sfx_merge.wav
```

---

## 20. 구현상 주의점 / 현재 코드 기준 특이사항

### 20.1 보스 웨이브 UI와 실제 보스 생성 조건 불일치

현재 HTML 기준:

- 실제 보스 생성: `wave % 4 === 0`
- UI 보스 웨이브 표시: `wave % 5 === 0`

따라서 보스가 실제로 나오는 웨이브와 UI가 `보스 웨이브`라고 표시하는 웨이브가 다를 수 있다.  
추후 반드시 통일하는 것이 좋다.

### 20.2 일부 이전 구조의 잔재

현재 레벨업 스킬은 유닛 개별 성장으로 바뀌었다.  
다만 코드에는 과거 전역 강화 구조에서 사용하던 일부 `mods` 필드가 남아 있다.

현재 남아 있는 필드:

```text
mods.reward
mods.repair
mods.treasure
```

실제로 현재 주요 성장 구조는 `Planet.aug`와 `Planet.skillLevels` 중심이다.

### 20.3 장판 중심 난이도

v21 기준으로 난이도는 v20보다 소폭 낮아졌지만, 여전히 장판 중심이다.

- 공허판 배치만으로는 화력이 부족하게 설계됨
- 초반부터 장판 활용이 필요함
- 단, v20보다 1-1 초반 자원과 HP가 완화됨

---

## 21. 향후 개선 제안

HTML 기준 정리에서 벗어나지 않는 범위에서, 다음 개선이 필요하다.

### 21.1 보스 웨이브 조건 통일

다음 중 하나로 통일 권장.

```text
실제 보스 생성도 5웨이브마다
또는
UI 표기도 4웨이브마다
```

### 21.2 밸런스 테이블 분리

현재 수치가 JS 내부에 직접 들어 있다.  
향후 다음 데이터를 JSON으로 분리하면 유지보수가 쉬워진다.

- 행성 스탯
- 몬스터 스탯
- 장판 보너스
- 스킬 트리
- 스테이지 효과
- 웨이브 생성 규칙

### 21.3 스킬 선택 UI 개선

현재 스킬 선택지는 3개가 제공되며, 특정 행성 1개만 강화된다.  
추후 카드에 다음 정보를 더 명확히 표시하면 좋다.

- 강화 대상 행성의 현재 위치
- 기존 스킬 레벨
- 강화 후 예상 변화량
- 장판 위에 있는 행성인지 여부

### 21.4 장판 전략성 강화

현재 장판은 강한 보너스를 주지만, 앞으로 다음을 추가할 수 있다.

- 특정 행성 전용 장판
- 장판 간 시너지
- 장판 오염/정화
- 웨이브 중 장판 변형
- 보스가 장판을 잠시 무력화

### 21.5 히든 행성 연출 강화

현재 히든 스타 엔진은 조건 달성 시 자동 생성된다.  
연출적으로는 다음을 추가하면 좋다.

- 6개 행성에서 빛이 모이는 컷인
- 맵 전체 플래시
- 전용 사운드
- 잠금 카드 해제 애니메이션
- STAR BURST 발동 시 별폭발 전용 이펙트

---

## 22. 요약

`Planet Rift Defense: Cosmic Plates`의 현재 버전은 다음 방향으로 정리된다.

```text
우주 성역 방어
+ 행성 병기 랜덤 소환
+ 장판 중심 배치 전략
+ 같은 행성 병합
+ 낮은 보상/낮은 EXP 기반 난이도
+ 레벨업 시 아군 행성 1개만 랜덤 성장
+ 6종 기본 행성 전용 스킬 트리
+ 히든 스타 엔진 융합
+ 성역별 맵 전체 효과
+ 전술판 궁극기 3종
```

현재 게임의 가장 중요한 정체성은 **장판이 없으면 버티기 어려운 행성 디펜스**다.  
행성은 랜덤으로 나오고, 성장은 특정 행성 하나에만 붙기 때문에, 플레이어는 매 판 다르게 형성되는 제한된 자원을 바탕으로 **어떤 행성을 어느 장판에 놓을지** 계속 판단해야 한다.
